name: Aurora-Shell CI/CD

on:
  push:
    branches: [ main, dev ]
    tags: [ 'v*' ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # --- WINDOWS BUILD ---
  windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build EXE
        run: |
          choco install nsis -y
          $nsi = Get-ChildItem -Recurse -Filter "*.nsi" | Select-Object -First 1
          & "C:\Program Files (x86)\NSIS\makensis.exe" /V3 $($nsi.FullName)
      - name: Create Zip
        run: Compress-Archive -Path ./*.exe -DestinationPath ./Aurora-Shell-Windows.zip
      - uses: actions/upload-artifact@v4
        with:
          name: windows-pkg
          path: ./Aurora-Shell-Windows.zip

  # --- MAC BUILD: The Absolute Path Resolution ---
  unix-build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare Scripts
        run: |
          mkdir -p scripts
          cp ./installer.sh scripts/postinstall
          # Ensure aurora_theme.sh exists to prevent build-stopping errors
          cp ./aurora_theme.sh scripts/ 2>/dev/null || touch scripts/aurora_theme.sh
          chmod +x scripts/postinstall

      - name: Build User-Level PKG
        run: |
          brew install create-dmg
          mkdir -p pkg_root
          
          # 1. Component build
          pkgbuild --root pkg_root \
                   --scripts scripts \
                   --identifier com.aurora.shell.component \
                   --version "1.0.0" \
                   "Component.pkg"

          # 2. THE FIX: Absolute Path Injection
          # Captured dynamically to satisfy macOS security/pathing
          ABS_PATH=$(pwd)
          cat <<EOF > distribution.xml
          <?xml version="1.0" encoding="utf-8"?>
          <installer-gui-script minSpecVersion="1">
              <pkg-ref id="com.aurora.shell.component" file="file://$ABS_PATH/Component.pkg" auth="none"/>
              <options customize="never" require-scripts="true" hostArchitectures="x86_64,arm64"/>
              <choices-outline>
                  <line choice="default"/>
              </choices-outline>
              <choice id="default" visible="false">
                  <pkg-ref id="com.aurora.shell.component"/>
              </choice>
              <domains enable_anywhere="false" enable_currentUserHome="true" enable_localSystem="false"/>
          </installer-gui-script>
          EOF

          # 3. Productbuild with explicit mapping
          productbuild --distribution distribution.xml \
                       --package-path . \
                       "Install-Aurora.pkg"
          
          # 4. DMG Wrap
          mkdir -p dmg_content
          mv "Install-Aurora.pkg" dmg_content/
          create-dmg \
            --volname "Aurora-Shell" \
            --window-pos 200 120 \
            --window-size 600 450 \
            "Aurora-Shell-Mac.dmg" \
            "dmg_content/"

      - uses: actions/upload-artifact@v4
        with:
          name: mac-pkg
          path: ./*.dmg

  # --- RELEASE ---
  publish-release:
    needs: [windows-build, unix-build]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            *.zip
            *.dmg
          body: "## 🌈 Aurora-Shell Release [${{ github.ref_name }}]"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}